export enum Role {
	USER = "USER",
	SUBSCRIBER = "SUBSCRIBER",
	ADMIN = "ADMIN",
	SUPER_ADMIN = "SUPER_ADMIN",
}

export enum NotificationChannel {
	EMAIL = "EMAIL",
	SMS = "SMS",
	WHATSAPP = "WHATSAPP",
}

export enum Status {
	ACTIVE = "ACTIVE",
	INACTIVE = "INACTIVE",
}
